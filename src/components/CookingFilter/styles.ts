import { StyleSheet } from "react-native";

export default StyleSheet.create({
  cookingContainer: {
    flex: 1,
    paddingTop: 20,
  },
  checkboxContainer: {
    flexDirection: "row",
    marginBottom: 20,
  },
  label: {
    marginLeft: 8,
  },
});
